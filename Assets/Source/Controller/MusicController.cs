public class MusicController : ControllerBaseModel
{
    public SoundFxController AudioController
    {
        get => default;
        set
        {
        }
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
